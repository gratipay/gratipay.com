import os
from gratipay.utils import http_caching
asset_root = os.path.join(website.www_root, 'assets')
[---]

# Load order
files = [
    'jquery.min.js',
    'chosen.jquery.min.js',
    'tipr.min.js',
    'bootstrap-dropdown.js',
]

js = http_caching.concat_files(files, asset_root, ".js")
[---] via stdlib_format
{js}
